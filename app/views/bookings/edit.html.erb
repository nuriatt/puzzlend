<div class="container">

  <h2>Edit Booking for <%= @booking.puzzle.name %></h2>

  <%= bootstrap_form_for [@puzzle, @booking] do |f| %>
    <%= f.date_field :start_date %>
    <%= f.date_field :end_date %>
    <%= f.submit "Update" %>
  <% end %>
</div>
