
 <div class="container d-flex justify-content-around">
  <div class="container booking-form">
    <h2>Book <%= @puzzle.name %></h2>
      <%= bootstrap_form_for [@puzzle, @booking] do |f| %>
        <%= f.date_field :start_date %>
        <%= f.date_field :end_date %>
        <%= f.submit "Book" %>
      <% end %>
  </div>
  <div class="booking-photo d-flex justify-content-center">
    <%= cl_image_tag @puzzle.photo.key %>
  </div>
<div>
